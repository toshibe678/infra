input {
  beats { port => 5044 }
  tcp { port => 50000 codec => json_lines }
}

filter {
  # ここに grok/json/date などを必要に応じて追加
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
